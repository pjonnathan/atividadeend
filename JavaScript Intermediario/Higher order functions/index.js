/* var calcularAnodeNascimento = function(idade, diaNascimento , mesNasciemnto, imprimir) {
    const mesAtual = 2
    const diaAtual = 6
    let anodeNascimento = 2023 - idade
    if(mesNasciemnto >= mesAtual) {
        if(diaNascimento>diaAtual) anodeNascimento--
    }
    imprimir(anodeNascimento)
}

let imprimirAnodeNascimento = anodeNascimento => {
    console.log('Seu ano de nascimento é: ' + anodeNascimento)
}

calcularAnodeNascimento(33, 30 , 2, imprimirAnodeNascimento) */

let mult = (numero, multiplicado) => numero * multiplicado

console.log(mult(2,2))